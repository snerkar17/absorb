# backend

Supabase artifacts only — no custom server (see CLAUDE.md §7).

This will hold:
- `supabase/migrations/` — SQL migrations for `profiles`, `days`, `notes`, and RLS policies
- generated TypeScript types (`supabase gen types typescript`)

Empty until Phase 2 (Supabase setup).
